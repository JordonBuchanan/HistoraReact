import React from 'react';
import Spinner from './spinner.gif';

export default () => {
    return (
      <div>
        <i className="fa fa-spinner fa-spin" style={{fontSize: '24px', margin: 'auto', display: 'block'}}></i>
      </div>
    )
  }
